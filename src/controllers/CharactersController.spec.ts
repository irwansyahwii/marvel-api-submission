import { PlatformTest } from "@tsed/common";
// import { CharactersController } from "./CharactersController";

describe("CharactersController", () => {
  beforeEach(PlatformTest.create);
  afterEach(PlatformTest.reset);

//   const instance = PlatformTest.get<CharactersController>(CharactersController);

  describe("findAll()", ()=> {
    it("correctly set default value for offset", async () => {
        // await actual = instance.findAll(undefined, 100);
      
    });
    
  })

});
